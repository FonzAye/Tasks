server.host: "0.0.0.0"
elasticsearch.hosts: [{% for host in groups['all'] %}
{% if hostvars[host]['private_ip'] is defined %}
"http://{{ hostvars[host]['private_ip'] }}:9200"{% if not loop.last %},{% endif %}
{% else %}
{% set _ = errors.append(host) %}
{% endif %}
{% endfor %}]
server.publicBaseUrl: "http://{{ ansible_host }}:5601"
